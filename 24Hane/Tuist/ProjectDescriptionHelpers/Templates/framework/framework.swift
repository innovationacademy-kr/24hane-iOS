import ProjectDescription

let nameAttribute: Template.Attribute = .required("name")

let template = Template(
  desctiption: "A template for creating a new framework.",
  attributes: [
    nameAttribute,
  ],
  items: [
    
  ]
)